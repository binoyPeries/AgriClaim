enum UserRoles {
  farmer,
  officer,
}

enum ClaimStates {
  pending,
  inReview,
  completed,
}

enum LineIntersectionStates {
  intersectsWithLine,
  noIntersectsWithLine,
  onTheLine,
}

enum NetworkStatus {
  notDetermined,
  on,
  off,
}
