enum UserRoles {
  farmer,
  officer,
}

enum ClaimStates {
  pending,
  inReview,
  completed,
}

enum BoundaryStates {
  inBoundary,
  outOfBoundary,
  onBoundary,
}

enum NetworkStatus {
  notDetermined,
  on,
  off,
}
